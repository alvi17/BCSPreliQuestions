<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alvi17.bcspreliquestionsanswer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Question_Answer"/>
        <activity android:name=".QA"/>
        <activity android:name=".QA_Page"/>
        <activity android:name=".Exercise"/>
        <activity android:name=".Exercise_FronPage"/>
        <activity android:name=".ExamActivity"/>
    </application>

</manifest>
